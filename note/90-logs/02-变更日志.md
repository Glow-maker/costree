# 变更日志

## 2026-07-21 外部数据源模拟与两项目闭环演示

- 在后端新增 PostgreSQL `cost_demo_source` 独立模拟源 schema 和分步脚本，覆盖单位字典、主业项目树、工作令映射、预分预控项目单位金额、项目办/研制单位补充字段和账面凭证明细。
- 新增两个专用演示项目：单位管理层级项目和跨年度工作令项目；同步采用稳定源 ID 和业务唯一键，重复执行不重复记账。
- 新增事务校验、年度工作令借方账面快照重建、八项组成对账、贷方排除验证和定向清理脚本。
- 新增 `30-data/10-成本库数据源与测试导入闭环.md`，并更新 09 总设计方案和数据目录入口，明确预分预控四项金额与账面借方明细的权威边界。
- 修正既有 MySQL/PostgreSQL 单位层级演示脚本：预警清理通过 `project_id/work_order_id`，不再引用不存在的 `cost_warning_record.project_code`。

## 2026-05-17

- 建立 `note` 文档中心目录结构。
- 新增文档中心索引 `README.md`。
- 新增总览、调研、需求、数据、接口、前端、后端、测试、部署、过程记录目录。
- 新增第一批施工型文档骨架。
- 更新前端实际开发位置为 `<frontend-root>`。
- 确认后端实际开发位置为 `<backend-root>`。
- 完成第一轮现有中台代码分析，更新 `10-research/01-现有中台代码分析.md`。
- 完成参考模块清单，更新 `10-research/03-参考模块清单.md`。
- 固化 `Table 2`、`Table 3` 只是原型追溯编号的口径。
- 新增 `20-requirements/04-页面与角色拆分方案.md`。
- 更新 PRD、角色权限矩阵、数据模型、接口清单、前端迁移计划、后端设计，按项目办/研制单位拆分页面和权限。
- 新增 `80-deployment/00-环境账号与敏感信息.md`，集中记录外网 MySQL 测试环境连接信息和后续账号维护规则。
- 完成 `30-data/schema-mvp.sql` 和 `30-data/seed-mvp.sql` 初稿，覆盖成本库 MVP 7 张表和外网测试数据。
- 已在外网 MySQL 创建 `costree_mvp`，执行建表和种子数据脚本；验证 7 张表记录数正常，超支样本 5 条。
- 完成 `40-api/02-接口字段契约.md` 初稿，固定型号树、成本项目、项目基本情况、工作令基础信息、导入结果、预警接口字段。
- 完成 `40-api/03-接口联调用例.md` 初稿，按当前种子数据整理只读查询、CRUD、导入导出、状态和预警推送用例。
- 完成 `60-backend/02-Controller-Service-Mapper生成清单.md` 初稿，按批次拆分后端 Java 文件、权限点、错误码和首轮生成顺序。
- 完成 `60-backend/03-导入导出实现说明.md` 初稿，明确项目基本情况、工作令基础信息两个 Excel 模板、导入校验、`updateSupport` 和导入批次记录规则。
- 完成 `60-backend/04-预警与消息推送说明.md` 初稿，明确 10% 工作令超支预警公式、预警记录生命周期、MVP 模拟推送和中心消息后续确认项。
- 完成 `60-backend/05-首轮后端生成施工说明.md` 初稿，拆分 B0 只读检查、B1 模块骨架、B2 型号树和成本项目只读链路的生成提示词、文件边界和验收标准。
- 后端完成 B1：新增 `yudao-module-cost`、`yudao-module-cost-api`、`yudao-module-cost-biz` 模块骨架，并接入根 `pom.xml`。
- 后端完成 B2-1：实现 `GET /cost/model-node/tree` 型号树只读接口。
- 后端完成 B2-2：实现 `GET /cost/project/page`、`GET /cost/project/get?id=` 成本项目只读接口。
- 后端完成 B3-1：实现 `cost_project_basic` 项目基本情况 CRUD 第一版，覆盖分页、详情、新增、编辑、删除。
- 后端完成 B4-1：实现 `cost_work_order` 工作令基础信息 CRUD 第一版，覆盖分页、详情、新增、编辑、删除。
- 后端完成 B3/B4-2：实现 `cost_project_basic` 和 `cost_work_order` 两个维护对象的 `export-excel` 导出接口。
- 完成 HTTP 联调前检查：当前 `cost` 模块缺少独立运行配置，且现有 `data-server` 默认不扫描 `cost` 包，因此本轮未启动服务联调，待确认后端运行形态后再进行。
- 新增 `60-backend/06-后端实施记录.md`，记录后端批次进度、编译验证结果和待确认项。
- 后端验证：执行 `mvn -pl yudao-module-cost/yudao-module-cost-biz -am -DskipTests compile`，B1/B2/B3-1/B4-1/B3-B4-2 均编译通过；Maven 关于 `yudao-module-data-biz` 的 duplicate dependency 警告为既有项目警告。
- 确认成本库后端按 `cost-server` 独立启动推进，不挂入现有 `data-server`。
- 后端完成 B3/B4-3：实现项目基本情况、工作令基础信息的导入模板下载和 Excel 导入接口。
- 新增导入批次、导入错误明细的 DO、Mapper、Service 和统一返回 VO，导入结果写入 `cost_import_batch`、`cost_import_error`。
- 后端验证：再次执行 `mvn -pl yudao-module-cost/yudao-module-cost-biz -am -DskipTests compile`，B3/B4-3 编译通过；Maven 关于 `yudao-module-data-biz` 的 duplicate dependency 警告仍为既有项目警告。
- 后端完成 B5：新增 `cost-server` 独立运行配置 `application.yml`、`application-local.yml`，本地默认端口 `48108`。
- 后端补齐独立启动依赖：`yudao-spring-boot-starter-rpc`、`mysql-connector-j`，新增成本模块 `SecurityConfiguration`。
- 后端启动验证：`mvn -pl yudao-module-cost/yudao-module-cost-biz -am -DskipTests compile` 通过，`package` 通过，`java -jar` 启动成功。
- HTTP 验证：`GET http://127.0.0.1:48108/actuator/health` 返回 `{"status":"UP"}`；验证后已停止本地 `cost-server`。
- 更新 `60-backend/06-后端实施记录.md` 和 `80-deployment/00-环境账号与敏感信息.md`，记录 B5 启动链路、Redis 本机依赖和验证结果。
- 确认业务接口实际路径为 `/admin-api/cost/...`；外网单服务联调因缺少 `system-server` 的 token、租户、权限校验以及 `infra-server` 错误日志能力而阻塞。
- 后端新增默认关闭的 `COST_LOCAL_TEST_MOCK_PLATFORM` 本地联调开关，用于外网单独启动 `cost-server` 时绕过平台 RPC 依赖；内网集成时必须关闭。
- 后端修复项目基本情况导入字段映射：为 `competitorUnit1`、`competitorPrice1`、`competitorUnit2`、`competitorPrice2` 显式绑定数据库列 `competitor_unit_1`、`competitor_price_1`、`competitor_unit_2`、`competitor_price_2`。
- HTTP 联调通过：健康检查、型号树、项目基本情况模板下载/导入/分页/导出、工作令模板下载/覆盖导入/分页/导出均返回成功。
- 前端进入 Vue3 接入阶段：新增项目基本情况、工作令基础信息两个列表页，覆盖查询、分页、导入、导出和导入结果展示。
- 前端新增成本库 API 文件和通用导入弹窗，并在 `remaining.ts` 临时接入 `/cost` 静态路由。
- 前端局部验证：成本库新增 SFC parse/compile 通过，成本库相关 ESLint 通过；`pnpm run build:dev` 本轮 3 分钟未完成并超时终止，未生成 `dist`。
- 后端修正 `CostServerApplication` IDEA 本地启动口径：`application-local.yml` 默认连接外网 MySQL 测试库，并默认启用本地平台 Mock。
- 重新初始化外网 MySQL `costree_mvp`：执行 `schema-mvp.sql` 和 `seed-mvp.sql`，验证 7 张表数据量正常。
- 后端本地启动复核通过：健康检查、型号树、项目基本情况分页均返回成功；验证进程已停止，未占用 `48108`。
- 前端首页新增“成本树平台”入口卡片，跳转 `/cost/project-basic`。
- 修复前端本地联调成本库接口 `访问资源不存在`：开发期 `/admin-api/cost/**` 直接代理到 `cost-server:48108`，并注入本地联调用 `login-user`。
- 修复成本库导入弹窗上传地址，改为相对 `/admin-api`，保证导入接口同样走开发代理。
- 后端网关新增 `/admin-api/cost/**` 路由和 Knife4j `cost-server` 聚合入口，用于内网/正式中台集成。
- 完成 `80-deployment/01-外网到内网迁移说明.md` 内网部署用户手册，覆盖 MySQL 建表、YAML 配置、Nacos、网关、前端部署、菜单权限、验收和常见问题。
- 修复前端成本库本地代理租户越权：`/admin-api/cost/**` 开发代理强制统一 `tenant-id` 和模拟 `login-user.tenantId`，避免平台登录租户与外网测试库租户不一致。
- 成本库外网联调默认租户从 `1` 调整为内网真实租户 `124`：更新前端本地代理默认值、后端本地 mock 默认值、`seed-mvp.sql` 默认测试租户，并新增 `30-data/update-tenant-id-124.sql` 迁移脚本。
- 已执行外网 MySQL `costree_mvp` 租户迁移：`cost_model_node`、`cost_project`、`cost_project_basic`、`cost_work_order`、`cost_import_batch`、`cost_import_error`、`cost_warning_record` 均从 `tenant_id=1` 更新为 `tenant_id=124`。
- 明确当前成本库缺少原型中的前台汇总展示页、目录树选择、项目列表/详情、成本树详情、型号对比、预警等页面；现有两个页面属于后台维护页。
- 新增 `50-frontend/06-前台门户与后台管理执行清单.md`，把前台门户、后台管理、权限、验证和长线任务拆成可追踪清单。
- 前端新增 `/cost/index` 成本库汇总展示页、`/cost/catalog` 成本库目录树页。
- 前端将现有维护页面迁移到 `/cost/back/project-basic`、`/cost/back/work-order`，并保留旧路由兼容跳转。
- 首页“成本树平台”入口改为跳转 `/cost/index`。
- 前端新增 `/cost/tree-detail?projectCode=xxx` 成本树详情页第一版，从目录页项目列表进入，先基于项目和工作令数据实时构建“型号-系统-产品”树。
- 前端成本树详情页已通过 `pnpm run ts:check:cost` 和成本库局部 ESLint；Codex 测试浏览器因无登录态被中台登录守卫重定向到 `/login`，视觉效果待已登录态复核。
- 修正 `/cost/index` 总体展示交互：移除底部数据采集入口；点击饼图打开成本组成详情弹窗；“查看研制单位成本详情”按钮才打开研制单位明细表。
- 优化 `/cost/index` 领域卡片六边形指标布局：指标移入外圈安全轨道，移动端改为两列 KPI 卡片，避免文字和六边形描边重叠。
- 前端新增 `/cost/project-detail?projectCode=xxx` 项目详情页第一版，从目录页项目列表进入，复用成本项目、项目基本情况和工作令接口展示单项目上下文。
- 增强 `/cost/catalog` 目录页：新增关键词、领域、系列、批次、阶段筛选工具条；项目表新增本地收藏星标和“只看收藏”快速筛选，收藏编号暂存浏览器 `localStorage`。

## 2026-05-18

- 后端为项目基本情况和工作令基础信息新增轻流程状态接口：提交、确认通过、退回；第一版继续复用业务表 `status` 字段，不新增 BPM 流程表。
- 前端新增 `/cost/collect` 数据采集前台页，包含项目办填报、研制单位填报、待确认三个页签。
- 成本树平台顶部导航新增“数据采集”入口。
- 更新接口字段契约、前端接入记录、后端实施记录和前台执行清单，记录当前轻流程状态机和后续正式 BPM 升级点。

## 2026-05-21

- 前端 `/cost/index` 总体展示页新增“八院合计”中心展示块，复用现有四领域实时汇总数据前端聚合，不新增表和接口。
- 中心展示块展示合同金额、到款金额、目标金额、账面金额、到款率、执行率、结余金额、结余率和领域数据数量；结余率和“数据”业务口径仍需确认。
- 原有四个领域卡片、饼图详情和研制单位成本详情交互保持不变。
- 优化 `/cost/index` 中心合计块布局：由独立整行改为整体驾驶舱画布，中间为四个领域六边形围绕“八院合计”的集群，成本组成饼图分布在左右两侧；同时增加紧凑模式和视口滚动兜底。
- 优化中心集群视觉协调性：放大领域六边形，将领域指标收进六边形内部；八院合计金额和百分比格式调整为与领域卡片一致，避免数字省略号。
- 继续优化 `/cost/index` 中心集群：四个领域六边形同步放大外层容器，指标贴近六个角位；八院合计中心块缩小，底部结余金额、结余率、成本合计改为竖向排列，减少对下方领域节点的遮挡。
- 按参考图继续调整 `/cost/index`：四大领域指标改为参考中心合计的六点分布，八院合计中心块适当放大并居中，中心列宽和四领域间距同步调整，增强整体对称感。
- 修正 `/cost/index` 八院合计块视觉偏移：中心块整体右下补偿，减少偏上、偏左观感。
- 修正 `/cost/index` 分辨率变化后的布局挤压：调整三栏比例，给左右饼图栏更稳定空间，并让四大领域节点从画布边缘响应式内收。
- 调整 `/cost/index` 八院合计块定位：取消右下补偿，恢复到中间集群容器几何中心。
- 调整 `/cost/index` 八院合计块定位口径：按四大领域围合区域的视觉中心定位，水平居中、垂直下移，使八院六边形本体位于四大领域中间。
- 继续微调 `/cost/index` 八院合计块位置：根据截图反馈整体右下移动，修正偏左、偏上的视觉问题。

## 2026-05-22

- 根据任务书补充需求，调整 `/cost/tree-detail?projectCode=xxx` 成本树详情页：
  - 右上角新增多阶段选择，默认全部汇总；
  - 阶段切换按工作令最终阶段归集目标成本和账面成本；
  - 型号根节点同步展示当前阶段；
  - 树层级从“型号-系统-产品/工作令”改为“型号-单位”，工作令移入单位详情页；
  - 院外单位作为二级汇总节点预留，不展开工作令；
  - 型号右侧新增项目成本组成饼图，点击弹出组成详情。
- 后端 `GET /cost/overview/unit-detail` 新增 `projectCode`、`stageCodes` 过滤能力，支持成本树详情页按项目和阶段取真实成本组成数据。
- 新增设计记录 `50-frontend/07-成本树详情页阶段与单位视角设计.md`，记录阶段、单位、饼图数据来源和待确认项。
- 根据业务截图新增单位字典 `cost_unit_dict`，字段覆盖核算单位 ID/编号/名称、管理单位、往来单位、是否集团内、是否八院内、单位 ID。
- 后端新增单位字典只读接口 `GET /cost/unit-dict/list`，前端成本树用 `insideInstitute` 判断院内/院外。
- `/cost/tree-detail?projectCode=xxx` 主成本树页面收口为“型号 -> 单位”，不再在主页面展示第三层工作令。
- 新增 `/cost/tree-unit-detail`：
  - 院内单位点击后展示该单位下的工作令展开列表；
  - 院外单位汇总点击后展示所有院外单位信息和成本数据。
- 扩充外网测试数据：`ZY-2026-LA-001` 增加多个院内单位、多 M/C/Z 阶段、多工作令和多个无工作令的院外单位，用于验证阶段筛选、单位跳转和院外汇总。
- 已重灌外网 MySQL `costree_mvp`，当前核心记录数：`cost_unit_dict=21`、`cost_work_order=22`、`cost_unit_cost_detail=29`，其中 `ZY-2026-LA-001` 有 10 条工作令和 4 条院外成本明细。
- 验证通过：
  - `pnpm run ts:check:cost`
  - `pnpm exec eslint "src/api/cost/**/*.ts" "src/views/cost/**/*.vue" "src/router/modules/remaining.ts" "src/views/HomeIndex/index.vue" --ext .ts,.vue`
  - `mvn -pl yudao-module-cost/yudao-module-cost-biz -am -DskipTests compile`
- 前端提交：`a9b94d1 feat(cost): add pending allocation page`。
- 后端提交：`254ec08b feat(cost): add pending allocation work order api`。
  - `http://localhost/cost/tree-detail?projectCode=ZY-2026-LA-001` 返回 `200`
  - `http://localhost/cost/tree-unit-detail?...unitCategory=INNER` 返回 `200`
  - `http://localhost/cost/tree-unit-detail?...unitCategory=OUTER` 返回 `200`
- 对照业务方建表参考，新增源数据设计文档 `30-data/04-业务方建表参考对照与调整建议.md`，明确源系统表不直接替换成本库业务展示/填报表。
- `schema-mvp.sql` 新增 `cost_source_project_tree`、`cost_work_order_project_ref`、`cost_work_order_ledger_detail` 三张源数据表，分别承接主业项目树、工作令关联主业项目字典和账面成本凭证明细。
- `seed-mvp.sql` 补充业务方源表测试数据，并重灌外网 MySQL `costree_mvp`；当前新增记录数：`cost_source_project_tree=8`、`cost_work_order_project_ref=10`、`cost_work_order_ledger_detail=12`、`cost_import_batch=5`。
- 同步更新 `30-data/01-数据模型-MVP.md`、`30-data/02-枚举与测试数据方案.md`、`30-data/03-建表SQL说明.md`、`40-api/02-接口字段契约.md` 和 `50-frontend/07-成本树详情页阶段与单位视角设计.md`。
- 优化 `/cost/catalog` 成本库目录树页：
  - 左侧领域-系列-型号树改为“领/系/型”彩色单字层级标识；
  - 右侧项目列表保留表格展示，同时新增“列表/卡片”展示切换，卡片模式按项目摘要块展示；
  - 工作令明细表高度提升并显式开启表体滚动，避免明细区域不可滚动。
- 验证通过：
  - `pnpm run ts:check:cost`
  - `pnpm exec eslint "src/api/cost/**/*.ts" "src/views/cost/**/*.vue" "src/router/modules/remaining.ts" "src/views/HomeIndex/index.vue" --ext .ts,.vue`
- 修复 `/cost/catalog` 目录树页在全局 `body` 禁止滚动下无法向下查看工作令明细的问题：页面根容器改为固定视口高度并自身纵向滚动，卡片网格取消内部高度截断，左侧树和工作令表保留内部滚动。
- 验证通过：
  - `pnpm run ts:check:cost`
  - `pnpm exec eslint "src/api/cost/**/*.ts" "src/views/cost/**/*.vue" "src/router/modules/remaining.ts" "src/views/HomeIndex/index.vue" --ext .ts,.vue`
- 优化 `/cost/catalog` 工作令明细：改为前端本地分页展示，默认每页 8 条，支持 8/10/20/50 条切换；搜索、筛选、切换树节点和只看收藏时自动回到第一页。
- 验证通过：
  - `pnpm run ts:check:cost`
  - `pnpm exec eslint "src/api/cost/**/*.ts" "src/views/cost/**/*.vue" "src/router/modules/remaining.ts" "src/views/HomeIndex/index.vue" --ext .ts,.vue`
- 将 `/cost/catalog` 工作令明细从前端本地分页升级为后端分页：
  - `GET /cost/work-order/page` 新增 `keyword`、`projectCodes` 查询参数；
  - `GET /cost/work-order-ledger/summary` 新增 `workOrderNos` 查询参数；
  - 目录页只加载当前页工作令，并只对当前页工作令补账面成本汇总，避免一次性全量加载工作令和账面明细。
- 验证通过：
  - `pnpm run ts:check:cost`
  - `pnpm exec eslint "src/api/cost/**/*.ts" "src/views/cost/**/*.vue" "src/router/modules/remaining.ts" "src/views/HomeIndex/index.vue" --ext .ts,.vue`
  - `mvn -pl yudao-module-cost/yudao-module-cost-biz -am -DskipTests compile`

## 2026-06-02 成本库本地代理登录态修正

- 修正前端 `vite.config.ts` 中 `/admin-api/cost/**` 代理策略：成本库默认跟随 `VITE_BASE_URL` 访问业务中台，不再默认注入模拟 `login-user` 和覆盖 `tenant-id`。
- 新增显式开关 `VITE_COST_MOCK_LOGIN=true`：只有外网单独启动 `cost-server` 调试时才注入本地 `login-user` 和 `VITE_COST_LOCAL_TENANT_ID`。
- 该调整避免业务中台真实登录用户、角色、租户和数据权限被固定测试用户覆盖，后续权限验收应使用默认嵌入中台模式。
- 同步更新 `50-frontend/05-MVP前端接入记录.md`、`80-deployment/01-外网到内网迁移说明.md`、`RISKS.md` 和 `PROJECT_STATE.md`。
- 验证通过：
  - `pnpm run ts:check:cost`
  - `pnpm exec eslint "vite.config.ts" "src/api/cost/**/*.ts" "src/views/cost/**/*.vue" "src/router/modules/remaining.ts" "src/views/HomeIndex/index.vue" --ext .ts,.vue`
- 前端提交：`f4fb85f fix(cost): gate local mock login proxy`

## 2026-06-02 项目状态和计划跟进

- 更新 `PROJECT_STATE.md`：把当前目标统一为嵌入“业务中台”，补充真实登录态代理策略，修正前后端仓库当前状态。
- 重建 `PLAN.md`：把已完成的代理登录态修正和账面聚合第一版从待办中移出，下一步聚焦真实登录态页面复核和 `/cost/analysis` 统计分析第一版。
- 更新 `DECISIONS.md`：新增 D015“前端代理登录态”决策索引。
- 更新 `README.md`：同步当前阶段为 MVP 实现、原型迁移和权限链路收口。

## 2026-06-02 全栈边做边学与 Nacos/Redis 说明

- 新增 `00-overview/02-全栈边做边学与运行链路说明.md`，解释当前成本库请求链路：前端 `/cost/...`、网关 `/admin-api/cost/**`、Nacos 服务发现、`cost-server`、MySQL。
- 明确 Nacos 在本项目中的核心作用是服务注册/发现，网关通过服务名 `cost-server` 找到成本库后端实例。
- 明确 Redis 只作为缓存和中台基础设施，不保存成本库正式业务主数据。
- 记录 `Unable to find instance for cost-server` 的排查顺序：先查 `cost-server` 是否启动，再查 Nacos discovery、namespace/group、网关路由和服务重启。
- 更新 `README.md`、`PROJECT_STATE.md`、`PLAN.md`、`RISKS.md`、`DECISIONS.md`、`80-deployment/01-外网到内网迁移说明.md`、`50-frontend/05-MVP前端接入记录.md` 和 `60-backend/06-后端实施记录.md`。
- 同步当前运行链路修正：
  - 前端 `src/main.ts` 恢复初始化失败日志输出；
  - 后端 `application-jt.yml` 默认启用 `cost-server` Nacos discovery，并与网关保持 Nacos 账号口径一致。
- 前端提交：`8c7d21b fix(cost): log app setup failures`。
- 后端提交：`6574e231 fix(cost): enable jt nacos discovery`。
- 本轮运行态验证：
  - `GET http://127.0.0.1:38108/actuator/health` 返回 `{"status":"UP"}`；
  - `GET http://127.0.0.1:38080/admin-api/cost/overview/summary` 返回 `401 账号未登录`，不再返回 `Unable to find instance for cost-server`。

## 2026-06-02 成本树代码阅读地图与自主开发学习路线

- 新增 `00-overview/03-成本树代码阅读地图与自学路线.md`，把成本树当前前端页面、前端 API、后端 Controller/Service/Mapper/DO、数据库表和请求链路串成阅读导航。
- 文档按 `/cost/index`、`/cost/catalog`、`/cost/project-detail`、`/cost/tree-detail`、`/cost/tree-unit-detail`、`/cost/collect`、`/cost/warning`、后台维护页逐块说明新增内容和阅读顺序。
- 文档补充自主新增功能的固定流程：需求确认、数据表确认、后端接口、前端 API、Vue 页面、路由菜单、权限、测试和文档更新。
- 更新 `README.md` 当前重点阅读入口，更新 `PROJECT_STATE.md` 下一步最小任务，更新 `PLAN.md` 将代码阅读地图列为已完成且后续持续维护的学习任务。
- 本轮只修改 Markdown 文档，不改前端代码、后端代码和数据库。

## 2026-06-03 待分配池页面与接口

- 新增待分配业务口径：`cost_work_order.product_short_name` 为 `NULL`、去空格后为空字符串或等于 `待分配` 时，该工作令进入待分配池。
- 后端扩展工作令查询参数 `pendingAllocation=true`，并新增：
  - `GET /cost/work-order/pending-allocation/summary`
  - `GET /cost/work-order/pending-allocation/unit-detail`
- 待分配池账面成本优先使用 `cost_work_order_ledger_detail` 实时聚合，无明细时使用 `cost_work_order.book_cost_amount` 兜底。
- 前端新增 `/cost/pending-allocation` 页面，顶部导航新增“待分配池”入口；页面包含指标、筛选、单位汇总、后端分页主表和单位抽屉穿透。
- 数据采集前台页的产品简称字段支持选择或输入 `待分配`，保留自由输入能力。
- `seed-mvp.sql` 增加 4 条待分配工作令样本和 4 条账面明细样本，覆盖 `NULL`、空字符串、`待分配`、院内/院外、正常/超支场景。
- 已重灌外网 MySQL `costree_mvp`，当前关键记录数：`cost_work_order=26`、待分配工作令 `4`、`cost_work_order_ledger_detail=16`。
- 同步更新 `30-data/01-数据模型-MVP.md`、`40-api/02-接口字段契约.md`、`50-frontend/08-待分配池页面设计.md`、`README.md`、`PROJECT_STATE.md` 和 `PLAN.md`。
- 验证通过：
  - `pnpm run ts:check:cost`
  - `pnpm exec eslint "src/api/cost/**/*.ts" "src/views/cost/**/*.vue" "src/router/modules/remaining.ts" "src/views/HomeIndex/index.vue" --ext .ts,.vue`
  - `mvn -pl yudao-module-cost/yudao-module-cost-biz -am -DskipTests compile`

## 2026-06-03 后端分页与聚合风险治理

- 治理待分配池全量加载风险：
  - `pending-allocation/unit-detail` 改为数据库分页；
  - 账面明细只聚合当前页工作令；
  - `pending-allocation/summary` 改为按单位 SQL 聚合。
- 治理预警中心全量加载风险：
  - `GET /cost/warning/page` 改为 SQL 实时计算超支并数据库分页；
  - `cost_warning_record` 仍只保存推送和处理状态。
- 治理总体展示内存聚合风险：
  - `overview/summary` 改为按领域 SQL `GROUP BY`；
  - `overview/unit-detail` 改为按领域和单位 SQL `GROUP BY`。
- 增加同步导出保护：
  - 项目基本情况和工作令导出超过 5000 行时返回明确错误，要求缩小筛选范围；
  - 后续如需大批量导出，再新增异步导出任务。
- 同步更新 `PROJECT_STATE.md`、`PLAN.md`、`RISKS.md`、`40-api/02-接口字段契约.md` 和 `60-backend/06-后端实施记录.md`。
- 验证通过：
  - `mvn -pl yudao-module-cost/yudao-module-cost-biz -am -DskipTests compile`
  - `pnpm run ts:check:cost`
  - `pnpm exec eslint "src/api/cost/**/*.ts" "src/views/cost/**/*.vue" "src/router/modules/remaining.ts" "src/views/HomeIndex/index.vue" --ext .ts,.vue`
  - 外网 MySQL `costree_mvp` 等价 SQL 验证：待分配汇总 4 个单位、预警实时超支计数 15、总体展示 4 个领域。

## 2026-06-03 长期项目文档与工程经验沉淀模式

- 新增 `00-overview/05-长期项目文档与工程经验沉淀模式.md`，把当前项目采用的“文档中心 + 状态台账 + 工程经验手册 + 变更追溯”方式整理成后续项目可复刻流程。
- 文档明确新项目启动、每轮开发闭环、工程经验沉淀格式、新需求输入模板、AI 协作方式、维护节奏和复刻清单。
- 更新 `README.md` 当前重点阅读和维护规则，加入该模式文档入口。
- 本轮只修改 Markdown 文档，不改前端代码、后端代码和数据库。

## 2026-06-12 新对话接管成本库项目启动指南

- 新增 `00-overview/06-新对话接管成本库项目启动指南.md`，明确新 Codex/ChatGPT 对话接管成本库项目时应先读 `.agent-handoff`、再读 `note`、按需读 `docs`。
- 文档提供同机 Codex 新对话可直接复制的启动 prompt，以及外部 ChatGPT 无法访问本机路径时应提供的文件清单。
- 文档按前端、后端、数据同步、项目管理四类任务补充分流阅读路径，并列出建议调用的 skills。
- 更新 `README.md` 当前阶段、当前重点阅读和维护规则，加入新对话接管指南入口。
- 本轮只修改 Markdown 文档，不改前端代码、后端代码和数据库。

## 2026-06-12 长期文档维护与工作区活力机制

- 更新 `00-overview/05-长期项目文档与工程经验沉淀模式.md`，补充“文档作为项目操作系统”的长期维护机制。
- 明确每轮开发的固定节奏：开工 5 分钟恢复上下文，新需求先规格化，开发中按专题同步文档，收尾 10 分钟更新状态、计划、变更日志和交接信息。
- 补充每周 30 分钟文档巡检和阶段复盘要求，避免 `PROJECT_STATE.md`、`PLAN.md`、`RISKS.md`、`DECISIONS.md` 和 README 过期。
- 补充开工恢复、新需求规格化、收尾沉淀和文档健康检查 4 类可复用 prompt。
- 同步更新 `README.md`、`PROJECT_STATE.md`、`PLAN.md` 和 `AGENTS.md`，把长期活文档机制纳入成本库项目治理规则。
- 本轮只修改 Markdown 文档，不改前端代码、后端代码和数据库。

## 2026-06-12 页面复核与本地运行链路收口

- 确认当前本机实际路径：文档仓库 `<costree-root>`，前端仓库 `<frontend-root>`，后端仓库 `<backend-root>`。
- 使用 `VITE_BASE_URL=http://127.0.0.1:38080` 且不启用 `VITE_COST_MOCK_LOGIN` 启动前端 dev server，复用业务中台真实网关链路访问成本库页面。
- 清理一批非成本页面中的无效自动导入，修复 Vite 启动阶段 unresolved import，包括误引入的 `dist-prod/assets/...`、`element-plus/es/locale`、`element-plus/es/utils` 等。
- 修正 `src/main.ts` 中不存在的 `Logger.error` 调用，避免应用初始化异常时再触发二次运行错误。
- 浏览器复核通过：
  - `/cost/pending-allocation` 显示 4 条待分配工作令、4 个涉及单位、目标成本 2,700.00、账面成本 3,000.00、超阈值预警 2；单位抽屉可展示 `WO-SP-001-03` 明细。
  - `/cost/warning` 显示预警总数 15、超阈值 6、待推送/失败 8、已处理 0。
  - `/cost/catalog` 显示 4 个领域/系列/型号树、8 个项目、26 条工作令；列表/卡片切换可用。
- 验证通过：
  - 前端 `pnpm run ts:check:cost`
  - 前端本轮触达文件 ESLint，0 errors、10 warnings
  - 后端 `mvn -pl yudao-module-cost/yudao-module-cost-biz -am -DskipTests compile`
  - 前端和后端 `git diff --check`
- 同步更新 `README.md`、`AGENTS.md`、`PROJECT_STATE.md` 和 `PLAN.md`，把页面复核状态推进到已完成；下一步最小任务改为 `/cost/analysis` 第一版迁移。

## 2026-06-12 模块化开发边界约束

- 明确成本库后续按模块化边界开发：默认只修改成本库/成本树相关文件。
- 前端默认只改 `src/views/cost/**`、`src/api/cost/**`、成本库路由/菜单入口和成本库复用组件。
- 后端默认只改 `yudao-module-cost/**`、`cost-server`、网关中成本库路由和必要配置。
- 如因全局构建、Vite 启动、依赖错误、登录态或网关集成必须修改其他模块，必须先通知用户说明原因、影响范围和替代方案，并在本轮文档中记录触碰文件、触碰原因、是否临时和回归方式。
- 同步更新 `AGENTS.md` 和 `00-overview/04-工程经验与开发约束手册.md`，作为后续 AI/开发者执行规则。

## 2026-06-18 文档路径可迁移口径

- 将 `note` 活跃文档中的本机绝对路径统一改为 `<costree-root>`、`<frontend-root>`、`<backend-root>` 或相对路径，避免在陵台计算机、内网部署机或其他开发机上无法识别。
- 在 `README.md`、`AGENTS.md` 和 `00-overview/06-新对话接管成本库项目启动指南.md` 中明确当前推荐相对布局：前端 `../sqlbot_with_bcback/costree-frontend`，后端 `../sqlbot_with_bcback/baback`。
- 在 `00-overview/00-文档结构与维护规范.md` 新增路径书写规则：当前操作指南、部署说明、接管 prompt 不写死 `H:\`、`D:\`、`C:\Users\...` 等本机路径。
- 更新 `DECISIONS.md`，新增 D020“文档路径可迁移口径”。
- 本轮只修改 Markdown 文档和交接状态，不改前端代码、后端代码和数据库。

## 2026-06-21 内网源表与成本库业务表关系方案

- 新增 `30-data/05-内网源表与成本库业务表关系实施方案.md`，明确主业项目树、工作令关联主业项目字典、项目工作令账面成本明细、单位字典与成本库业务表的关系。
- 新增 `30-data/06-内网原表-主业项目树-ads_lc_lshsxm2022.md`，按内网截图整理第一张原表 `sc_8001_cw.ads_lc_lshsxm2022` 的字段清单、初步理解、到 `cost_source_project_tree` 的候选映射和待确认问题。
- 新增 `30-data/07-内网原表-工作令关联主业项目字典-dwd_bd_bfcustomitem_gzl.md`，按内网截图整理第二张原表 `dwd_bd_bfcustomitem_gzl` 的字段清单、工作令到主业项目的候选映射和待确认问题。
- 新增 `30-data/08-内网原表-项目工作令账面成本明细-dws_bu_pz_pzmx_gzl.md`，按内网截图整理第三张原表 `sc_8001_fidw.dws_bu_pz_pzmx_gzl` 的字段清单、到账面明细标准表的候选映射和待确认问题。
- 新增 `30-data/09-成本库内网数据对接总设计方案.md`，面向项目交接和执行汇总内网原表、成本库标准源表、业务维护表、页面展示/实时聚合的设计表、对应关系、导入维护步骤和两个型号试点建议。
- 补充 `30-data/09-成本库内网数据对接总设计方案.md` 的字段级表结构说明，按“字段名、类型、中文说明、来源/维护方式、关联或页面用途、备注/待确认”整理内网原表、标准源表、业务表和辅助表；本次只改文档，不改前端、后端和 SQL。
- 固化当前建议：内网源表不直接替代页面业务表，采用“源数据层 -> 业务维护层 -> 展示/聚合层”三层口径。
- 明确后台可以做统一导入中心，但不同导入类型落不同表：单位字典、源项目树、工作令映射、项目基本情况、工作令基础信息、账面明细分表落库、分流程解析。
- 明确首轮导入顺序：单位字典、主业项目树、解析项目树、工作令映射、解析工作令、项目/工作令填报数据、账面明细、解析账面明细。
- 记录一个内网落地风险：`note/30-data/schema-mvp.sql` 已有 `cost_source_project_tree` 和 `cost_work_order_project_ref`，但最新后端部署脚本 `sql/mysql|postgresql/costree-cost.sql` 当前未包含这两张源表，内网建库前需决定补齐源表或使用内网原表/视图。
- 同步更新 `30-data/README.md`、`README.md`、`PROJECT_STATE.md`、`PLAN.md` 和 `RISKS.md`。

## 2026-07-22 PostgreSQL 内网统一迁移包

- 新增 PostgreSQL 结构版本 `20260722`，空库完整 DDL 会写入 `cost_schema_migration`。
- 新增旧库统一迁移目录 `sql/postgresql/costree-deploy/`：迁移前检查、单事务升级、升级后强校验和 Windows/Linux 执行入口。
- 统一升级脚本覆盖项目基本情况、单位成本、单位字典、逻辑工作令、账面明细唯一键，以及仅借方账面快照重建；发现业务键、源明细或固定金额冲突时整体回滚。
- 新增交付包构建与 SHA-256 完整性校验脚本，正式包默认不包含 seed 和演示数据，并记录后端 Git 提交号。
- 新增 `80-deployment/02-PostgreSQL内网升级与版本一致性.md`，将空库、旧库、数据导入、上线验收和回滚路径集中为当前操作入口。
- 本机 PostgreSQL 临时库验证通过：最新 DDL 空库建库、旧版 DDL 升级、跨年工作令合并、借贷排除、元/万元校正、预警引用迁移和升级脚本重复执行；测试完成后删除临时库。
- 后端 Reactor 编译、`CostMapperAnnotationSqlTest`、前端成本模块类型检查、采集页定向 ESLint 和迁移包 SHA-256 校验通过。
