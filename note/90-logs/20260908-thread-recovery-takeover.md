# 旧任务历史核对与接管

日期：2026-09-08。任务：优化八院合计布局，ID `01a064f9-a115-7b23-9406-9a85b62f843f`。

本轮只读任务历史、本地原始记录、故障日志与三个仓库状态，新增本摘要。未修改 Codex 会话文件或数据库，未执行旧对话中的历史提交、构建、部署指令。

## 历史可用性

任务读取接口可返回早期首页布局、指标、型号比对、填报页面及 20260824 离线包记录，但最新返回的回合停在 0907 型号比对修改过程，标记 interrupted。不能据此认定后续工作没有完成。

只读发现同一任务两份本地记录：

- `C:/Users/Tao/.codex/sessions/2026/09/03/rollout-2026-09-03T09-54-36-01a064f9-a115-7b23-9406-9a85b62f843f.jsonl`：20511 行。
- `C:/Users/Tao/.codex/sessions/2026/09/07/rollout-2026-09-07T04-20-55-01a064f9-a115-7b23-9406-9a85b62f843f_01a07861-932d-7d02-bd5c-b1f3bf5bd98b.jsonl`：2556 行，包含接口未返回的后续工作。

两份文件逐行 JSON 解析均无错误。这只能证明现存文件可解析，不能证明没有历史消息缺失。

现场日志 `C:/Users/Tao/AppData/Local/Codex/Logs/2026/09/07/codex-desktop-b132422b-15a7-4e60-976f-f33683d95c19-10780-t0-i1-111043-0.log:711` 实际记录：

```text
failed to prepare paginated fork
thread history projection for 01a07861-932d-7d02-bd5c-b1f3bf5bd98b expected ordinal 20669, got 20668
```

这证实聊天分支创建失败涉及历史投影序号不一致。它与段落不显示可能相关，尚未确定全部成因，也未修复应用历史。

## 补回的工作脉络

以下行号均指 0907 的第二份原始记录，时间为北京时间。

| 原始行号 | 内容与接管含义 |
|---:|---|
| 318、375 | 型号比对精简属性并新增院内外单位合同/账面比对与导出；catalog 使用填报用户；首页口径、布局和模型点击修改。旧答复记录前端 5e4f5864、后端 494009ab 已提交推送，页面实测仍有限制。不是当前待重新执行的需求。 |
| 434 | 去掉“数据舱”模式，保留经典、3D舱、平台舱；旧答复记录 cbfdebed 已推送。 |
| 1684 | 16:11 的一版包包含 system，记录基线后端 7469e960、前端 e95d87e9、根仓库 2cd69ddb。该交付形态被后续要求替代。 |
| 1691、1748 | 用户要求仅留现场必用、去掉 system 和重复资源；当时专员功能尚不能仅部署 cost 使用。这是过渡状态。 |
| 1777、1820、1827 | 用户确认专员功能也须只部署 cost，并同意继续调整为成本 PG 保存四类业务角色及范围，中台保留账号、登录及其他模块职责。 |
| 2390 | 19:01 完成最终 0907 精简候选包；82 项后端测试、前端构建、PG9.2 复验及包校验通过；新增本地角色源码未提交，真实验收 pending。ZIP SHA256 为 FA8412A3A0A34B055C1BDAD5EC3EEB37885F73402ADC41D80EA32B38758897C6。 |
| 2493 | 记录聊天分支失败及历史投影错误，不是业务 Git 分支失败。 |
| 2541、2553 | 已写项目交接，要求新任务读取交接、先审查三仓并报告，不提交、不重打包、不修改真实数据库及会话文件。 |

上述“测试/推送通过”是历史记录陈述。当前 HEAD 与交接基线已再次核实；本轮未重新访问远端、运行测试或重新验包。候选包一致性以本任务上一轮实际验包记录为依据。

## 当前接管基线

- 根仓库 `H:/light/project/costree`：`jt/cost-server-offline-20260907`，HEAD `2cd69dd`。
- 后端 `H:/light/project/sqlbot_with_bcback/baback`：`feature/costree`，HEAD `7469e960`，20 项已跟踪修改、8 项未跟踪新增。
- 前端 `H:/light/project/sqlbot_with_bcback/costree-frontend`：`costree`，HEAD `e95d87e9`，8 项已跟踪修改、2 项未跟踪新增；design-qa.md 仍保留本地。
- 新增本地授权源码未提交推送；没有未提交的 system 修改。已有业务改动、配置、备份保持原状。

当前规则：仅部署 cost 与前端，不部署 system、不直接写 DM；专员标识 cost_role_allocator，只管理四类成本角色及范围；管理员保留原管理页面。无本地角色记录才沿用平台旧角色，NULL 本地角色是显式撤销。旧 JAR 不理解撤销记录，不能盲目回退后开放访问。

本任务上一轮比旧对话多完成了源码与制品审查，见 [20260907-local-authorization-review.md](20260907-local-authorization-review.md)。当前待办仍是：

1. R1：范围档案可能组合旧范围与新角色版本；R2：范围保存不递增版本，可能静默覆盖。两项为静态分析发现，待定向复现与修复。
2. 真实旧中台 RPC 的租户隔离、跨部门查询和性能验证。
3. 真实 DWS 8.2、管理员/专员/四类业务角色验收。
4. 后续根据用户明确指令选择性提交推送、决定是否另出新包。旧对话早期的“提交并推送”不作为本轮执行授权。

恢复依据优先级：本任务当前约束与最新仓库证据 → 最新交接和审查报告 → 旧对话已确认的最终决定。旧版 system/DM 部署流程、旧哈希及未完成过渡方案不得覆盖最终状态。
