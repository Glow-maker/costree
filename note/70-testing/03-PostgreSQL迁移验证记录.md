# PostgreSQL 迁移验证记录

验证日期：2026-07-22

目标结构版本：`20260722`

## 1. 验证范围

- 新建空库执行最新 `costree-cost.sql`。
- 旧版成本库执行迁移前检查、统一升级和升级后验收。
- 统一升级脚本重复执行，验证幂等性。
- 验证跨年度工作令合并、账面元/万元转换、仅借方统计和历史预警引用迁移。
- 验证数据库交付包的 SHA-256 完整性。

## 2. 旧库样本

在本机 PostgreSQL 临时数据库中使用早期离线包 DDL 建库，并插入：

- 同一项目单位的 `M/C` 两条 `cost_seed` 单位金额记录。
- 同一项目、单位、工作令编号的 2023、2024 两条年度工作令。
- 借方 100 万元、借方 200 万元、贷方 50 万元三条账面明细。
- 一条引用旧工作令 ID 的预警记录。
- 一条故意写错 `amount_wan` 的明细，用于验证元/万元校正。

## 3. 结果

| 检查项 | 结果 |
|---|---|
| 最新完整 DDL 在空库执行 | 通过 |
| 空库执行 `20-verify.sql` | 通过 |
| 旧库执行 `00 -> 10 -> 20` | 通过 |
| 同一升级脚本第二次执行 | 通过，记录数和金额不增加 |
| 年度工作令合并 | 2 条合并为 1 条逻辑工作令 |
| 工作令账面快照 | 300.00 万元 |
| 贷方 50 万元 | 保留明细，不进入账面快照 |
| `amount_wan` | 按 `amount / 10000` 重新生成 |
| 账面明细和预警工作令引用 | 指向保留的逻辑工作令 |
| 交付包 `verify-package.ps1` | 通过 |

临时数据库在测试完成后由测试程序执行删除，不使用现有 `costree_mvp` 业务库。

## 4. 工程回归

- `mvn -pl yudao-module-cost/yudao-module-cost-biz -am -DskipTests compile`：通过。
- `CostMapperAnnotationSqlTest`：1 个测试通过，0 失败。
- `pnpm run ts:check:cost`：通过。
- 采集页分页相关文件定向 ESLint：通过。
- 前后端及文档 `git diff --check`：通过。

## 5. 仍需在内网执行

本次验证证明脚本能覆盖当前空库和已知旧版结构，但不能替代内网真实数据检查。正式迁移仍必须：

1. 对真实库执行 `00-precheck.sql`。
2. 处理真实业务键重复、固定金额冲突和缺失源明细 ID。
3. 完成备份后执行统一升级。
4. 以 `20-verify.sql` 全部为 0 作为上线门槛。
