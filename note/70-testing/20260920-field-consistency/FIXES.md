# 字段一致性缺陷修复记录（2026-09-20）

F01–F08 已在源码中修复，并完成下述本地验证。前端 ef11293e 已推送至 codeup/feature/costree2，后端 3b3ac419 已推送至 codeup/feature/costree。未重启服务或制作离线包；现有运行进程和历史制品不会自动包含这些修改。

## 修改结果

| 项目 | 修复方式 | 保留的边界 |
|---|---|---|
| F01 详情新增与最新记录冲突 | 详情基本情况改为只读，移除新增/保存/提交分支，提示前往数据采集。旧 create 接口必须具备完整项目级必填信息，旧 submit 接口复用采集提交校验，防止旧入口绕过。 | 隐藏字段不构成缺陷，也没有在详情中增补显示型号简称、配套数等字段。采集 office-form 仍可保存未完成草稿。 |
| F02 导入必填规则过时 | 隐藏的附件/发射车字段不再必填；运载允许空分系统，其他领域继续要求分系统；配套数、型号简称、阶段与采集提交要求一致。 | 历史附件字段仍能被旧模板读取。缺少当前必填信息的旧文件会明确报错，不会自动编造数据。 |
| F03 导入金额被静默丢弃 | 新模板移除合同金额、目标成本、审定金额三列及样例值；旧文件带这些金额时，返回 warnings，并在导入结果弹窗提示它们不参与导入。 | 保持合同同步、科研经费维护目标/审定金额的来源，不改为从基本情况导入覆盖。 |
| F04 平台预填来源错误 | 删除详情新增时把 categoryName 放入 platformSeries 的代码。 | 型号树分类继续是分类，不冒充填报平台。 |
| F05 对比批次来源错误 | 对比页面和 Excel 导出均改读 batchCategory；填报保存、目录查询、对比查询共用 CostProjectReportingFields 派生规则。分类筛选中的领域判断同步兼容大小写和中文领域名。 | cost_project.batch_no 数据和 API 字段仍保留，业务类别/批次显示不再误用它。 |
| F06 阶段单选/多选不一致 | 详情以只读文本原样展示阶段集合，不再有单选写回路径；唯一交互填报入口沿用采集页多选、逗号保存。 | 旧单阶段和 M,C 等多阶段值不会被详情改写。 |
| F07 获取方式限制不一致 | 详情按原值只读展示，移除固定三选项；采集页继续自由输入。 | 自由填写与历史自定义值继续保留。 |
| F08 名称混用 | 基本情况/项目办 API 与 Excel 使用“型号简称”；工作令页面、待分配池、工作令 API/Excel 使用“工作令简称”；对比平台统一“平台/系列”。 | 基本情况与工作令 Excel 都兼容旧“产品简称”表头，新模板不再输出旧别名列。 |

同时保留上一轮“军科委”选项、目录平台/系列显示字段修复。

## 导入配套修正

运载项目允许空分系统后，查找既有记录使用 `subsystem_name IS NULL OR subsystem_name = ''`，避免 `= NULL` 查不到旧记录。覆盖导入继续根据项目、分系统、阶段找记录，更新原 ID，不盲目新增。

## 验证

- 后端 Maven `-Dtest=Cost*Test`：**115 项通过，0 失败，0 错误，0 跳过，BUILD SUCCESS**。
- 新增回归：新/旧 Excel 表头真实文件解析；新模板实际响应表头；旧金额提示和不入库；四领域数字编码的派生字段；对比导出字段；隐藏附件可空；运载空分系统、其他领域必填；配套数/简称缺失拒绝；旧新增/提交校验；重复导入更新原 ID；空分系统 SQL 构造。
- 两项原权限测试补齐必填业务数据后继续验证原来的状态保护和重复提交行为，没有删除断言或放宽权限。
- 前端 `node --test` 成本测试：**36 项通过**；`pnpm ts:check:cost` 与两仓 `git diff --check` 通过。
- 后端日志：[field-fixes-20260920-backend.log](H:/light/project/costree/.preview/field-fixes-20260920-backend.log)。

本次未新增或修改数据库结构，没有执行真实数据写入，也未修改收益率/执行率金额公式。既有 DDL.sql 工作区改动原样保留。

## 运行验收仍待完成

- 前后端需部署同一版本后，在浏览器确认详情只读、采集保存重开、目录/对比及其导出一致、导入警告显示。
- 真实内网 DWS、旧 RPC、六类账号验证仍须现场完成；本轮单元测试和类型检查不能替代。
- 草稿是否对普通查看用户可见、历史不完整记录应如何修复仍保持原规则，本轮未擅自选择历史值拼接或批量修改数据。
