# 20260910 系统性复核记录

当前结论：已完成下列源码修复和隔离 PostgreSQL 回归；**尚未完成最终离线包联合验收，不应据此宣布内网可发布**。

## 已修复问题

1. 成本实体主键依赖全局配置。在 `INPUT` 且未注册序列生成器的回归条件下，除了此前的角色表，还复现 `CostImportBatchDO.id` 为空。成本模块全部 17 个实体统一显式 `ASSIGN_ID`，保留调用方已提供的 ID；本轮新增修正 13 个实体。无需新增业务表、字段或重写存量 ID。
2. 首页展示取前四项、合计却包含所有领域，DEMO 可进入合计。入口统一筛选四个正式领域，保留真实编码，支持空间科学/空间安全；未知、重复或冲突映射明确报错。所有视图使用同一结果。
3. 3D 总体汇总漏传院内到账金额，其他院内金额缺失时被补零。补齐到账金额并保留未知值，已有收益率计算公式不变。

## 本轮证据与范围

|检查|结果|证据和限制|
|---|---|---|
|最终后端单元测试与 PG17 集成测试|103 项通过，0 失败/跳过|仓库根 `.preview/full-audit-all-tests.log`；92 项原有测试含 Mock，另 11 项为真实数据库集成测试，不能把 103 项全部称为端到端测试|
|PG9.2 主键与授权回归|12 项通过，0 失败/跳过|根 `.preview/full-audit-auth-pg92.log`；11 项集成加 1 项主键参数处理测试|
|17 个成本实体|实际 Mapper 插入、生成 ID、按 ID 回读、事务回滚通过|覆盖角色、三类范围、导入批次/错误、项目/基础信息、型号节点、单位/分系统字典、单位成本、工作令/明细、预警记录/接收人/操作日志；不等于所有业务流程的 HTTP 联调|
|角色生命周期|四类首次分配、12 种切换、撤销重授、旧版本拒绝通过|真实角色/锁/审计表；外部中台身份接口为明确的测试替身|
|范围保存|项目、领域、只选管理单位成功；非法项目回滚通过|真实 Mapper、租户拦截器、生产自动填充器、事务管理器|
|并发|首次角色并发、同范围版本并发、角色与范围交叉并发通过|最终无旧角色残留范围；共享 revision 与角色/范围事务一致|
|权限边界|普通全局查看不能保存授权、其他租户项目不能授予通过|中台用户是否归属租户仍须真实 RPC 验证|
|SQL|两版 PG 各 11 组场景通过|见 `sql-matrix.json`：空库备用建表、重复完整升级、未来版本标记下补表/字段、备用不改已有字段/版本、保留原数据、失败回滚、版本回滚、结构核对、非 public schema 隔离、列注释|
|前端|14 项领域/金额/动效回归通过；成本模块类型检查通过|源码测试；不替代最终 ZIP 的浏览器及六账号验收|
|源码对应关系|实际仓库与测试副本 173 个 Java 文件哈希一致|见 `tested-source-sha256.json`；不代表旧 JAR 包含本轮修复|

## 发布前必须完成

- 使用用户重新提供的前端压缩包；当前 `dist-prod.zip` SHA256 仍为 `fdc033af586c5e86bee9d0e47946def6887aba6244f1d656bdd215ea506a5172`，未包含本轮首页修复。
- 从本轮最终后端源码构建新的 cost JAR，与新前端 ZIP、已验证 SQL 一起核验哈希并执行联合验收。原 20260910/r1 离线包保持原样，不能当作本轮成果。
- 六类账号通过最终 HTTP/浏览器链路验证；真实旧中台的租户用户列表、身份/角色 RPC，以及真实 DWS 执行行为仍待验收。
- 采集保存、预警完整状态流转、查询导出须补最终应用级场景证据。本轮已有相应单元测试与数据库实体写入证据，不将两者相加当作完整流程实测。

## 环境与执行边界

本轮仅使用 `costree-full-audit-pg17`（55417）和 `costree-full-audit-pg92`（55492），数据库 `costree_audit`、`costree_sql_audit`。Docker 数据为本轮新建匿名卷，未挂载现有业务目录。没有写现有本地/内网数据库，没有部署或重启 system/cost，没有直接写 DM，没有提交推送或生成新离线包。

新增真实数据库测试：后端 `CostPostgresAuthorizationIT`；无显式隔离 JDBC 参数时跳过，指定参数只允许本轮端口及库名。通过 `-Dtest=CostPostgresAuthorizationIT,CostScopeIdGenerationTest -Daudit.jdbc.url=jdbc:postgresql://127.0.0.1:55417/costree_audit` 运行；PG9.2 端口为 55492。SQL 复验脚本 `tools/full-audit-20260910/verify_sql.py` 使用独立审查库，已存在同名审查库时主动失败，避免默默清理此前数据。

清单：`baseline.json`、`controller-inventory.json`、`entity-inventory.json`、`tested-source-sha256.json`、`sql-matrix.json`。后续以这些证据和实际源码继续，不以旧交付包版本号推断修复状态。
