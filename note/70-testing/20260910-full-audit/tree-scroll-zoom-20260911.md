# 20260911 成本树滚动与缩放

- tree-detail 与 tree-unit-detail 树区横向滚动条常显，调整高度避免横条落在屏幕下方。项目树桌面容器扣除现有中台顶部56px占位。
- 项目树 transform 缩放改为参与布局的 CSS zoom；单位树新增缩小/放大/100%重置，范围70%–130%、步长10%，边界禁用对应按钮。
- 当前项目 ZY-2026-DEMO-YEAR-002、电子所浏览器验证：单位树130%时拖动横条到 scrollLeft=83=max，70%下限禁用缩小，重置回100%；两页底部横条截图可见。项目树该单单位数据未超宽时无可拖拇指，超宽时按内容启用。
- vue-tsc 成本模块通过；两页模板及样式编译、diff空白检查通过。无数据库/后端更改、无提交打包。
