# 20260910 金额排版与填报文案调整

状态：源码修改、24 项前端回归、成本模块类型检查、16 个 Vue 模板/样式编译、1 项后端 Bean Validation 回归通过。浏览器工具连续返回 `Unable to load browser request-header policy`，本轮未完成页面截图、真实布局与点击验收。

## 已实现口径

1. 新增 FitNumber，按容器实际宽度缩小字体，保持完整金额在一行显示；应用总额卡片、六角指标、中央合计、3D/平台指标及成本树单位卡片。金额变化、容器尺寸变化和字体加载后重新测量。
2. 经典页顶部总额、3D 总体金额、3D/平台各领域总额卡片（含已有兼容展示）以亿元显示，万元值除以 10000，保留两位小数。经典页领域内金额及院合计结余仍为万元。每张总额卡明确单位；API 数值不变。
3. 经典中央圆从 326px 缩为 236px，只展示院内到款率、收益率、院内结余金额。四领域桌面布局居中，窄屏沿用顺序布局。金额蓝色、百分比绿色，负值红色，缺失值不当作负值。结余仍为院内到账减院内账面；收益率原公式保留。
4. 六角指标金额/百分比放大加粗、分别蓝色/绿色，负值红色。现有“收益率”名称保留，未修改公式。
5. 单位填报备注提示为“需补充说明内容”；合同合计移动到单位目标成本之前。
6. 审定金额在填报、项目/成本树/单位详情及研制单位详情表头增加提示图标：“验收项目以审计金额为准，审价项目以用户机关批复价格为准。”
7. 项目基本情况列表和编辑入口隐藏是否产品/附件/发射车；取消对应前后端必填校验，保留历史值。标题调整为院内目标成本执行率（院内账面/院内目标），计算不变。
8. 成本树排名改名院内单位目标成本执行率排名；原账面/目标与排序不变。
9. 节点详情隐藏分系统，产品简称显示为型号简称；字段、数据库内容不更名、不删除。
10. 成本树单位卡片金额加粗，并按可用宽度单行适配。

## 验证证据

- `.preview/presentation-frontend-tests.log`：24/24，通过。包括本轮亿元换算、缺失值、负数测试以及已有领域/金额/兼容展示回归。
- `.preview/presentation-types.log`：`pnpm ts:check:cost`，exit 0。
- 16 个修改涉及 Vue 文件经 compiler-sfc 解析及样式编译通过。
- `.preview/presentation-validation-backend.log`：隔离源码副本 Maven test，`CostProjectBasicValidationTest` 1/1，通过；不依赖数据库，验证隐藏字段可空、历史值可用、projectId 仍必填。
- 前端 `git diff --check` 通过。

## 运行与发布限制

本轮没有重新打包、提交推送、修改数据库或重启服务。后端唯一业务修改是 `CostProjectBasicSaveReqVO.productAttachmentType` 取消 NotBlank；已同步 `note/40-api/02-接口字段契约.md`。数据库业务列原本可空，不需要 DDL。Excel 导入校验未改变。

正在运行的旧 cost 进程没有加载该 DTO 变更，需重新编译并仅重启 cost 后验证新增/编辑保存。前端需要补真实浏览器检查：三模式单位显示、长金额不换行、中央圆与领域无重叠、负数红色、提示与隐藏入口。实际卡片像素效果尚未验收，不把源码/类型检查计为视觉验收。已有 3D 自动兼容源码已保留；原六账号、DWS、旧中台及最终包验收边界不变。
