# 0907 验证结果

发布 20260907-business-authorization；结构版本 20260820。此文记录本机验证，不代表真实内网验收。主流程为页面配置权限，DM写库SQL仅保留备用。

| 项目 | 结果 |
|---|---|
| 后端定向测试（含权限、预警、型号比对、Mapper SQL） | 成本69项＋中台专员9项，共78项通过，0失败 |
| 后端构建 | 当前源码 Maven clean package 成功，包含cost和system两个可执行JAR；分发件移除环境profile，固定密钥外置 |
| 前端 | 使用用户指定 ZIP；成本类型检查通过；不安装依赖、不重新构建 |
| 前端静态检查 | 成本类型检查、目标ESLint、5项首页口径测试通过；资源核对数量以验包器输出为准 |
| PostgreSQL 9.2.23 | 独立测试容器；合并升级连续执行、填报字段保留、已闭环预警保留、旧预警 LEGACY 迁移通过 |
| 升级兼容 | 模拟缺少领域/项目/单位授权表、分系统字典及预警子表后升级成功；普通预警索引修复成功 |
| 参数化 | 本轮使用隔离新库，public与124测试参数、错误schema及非法参数阻断通过；自定义schema和重复业务键曾于早先0907回归，本轮不重复声称执行 |
| 权限静态核对 | 4 类角色、21 权限、42 映射（包含 4 条目录映射）、4 通知模板；不创建/分配用户 |
| Redis | 本轮页面配置，不手工清理；备用清理脚本保留此前独立Redis验证，真实中台缓存失效待现场验收 |
| 参数生成器 | 参数替换、SQL无残留参数和事务处理测试通过；现场必用生成器仅生成成本库三份SQL，不生成DM写入SQL |
| DWS / DM8 | 静态检查；本机没有真实目标实例，未宣称执行成功 |

本次实际修补：原 PG92 完整结构将两个预警业务索引建为普通索引，导致原生 PostgreSQL 验收失败。新增 `16-repair-warning-indexes-20260907.sql`：原生 PG 补唯一保护，DWS 保留原兼容策略，不改业务行、不重置结构版本。

后端运行代码提交：`7469e960e4b2633e652923a90f0edf1335f1dcfd`。
前端提交：`e95d87e941d116afd3775dff5b5e2d5b72656ead`。
发布工具提交见 RELEASE-INFO，源码快照由 `SOURCE-MANIFEST.json`、`SHA256SUMS.txt` 追踪；不夹带本地交接、design-qa或测试数据。

前端原始 ZIP SHA256：`FB8DA4C7A96AFA63904CB7FFF1A0E3E82993E5EB214786845BAD781936472FAC`。
两个分发 JAR 哈希见 `RELEASE-INFO.txt`。未启动分发JAR连接真实中台/成本库，真实专员登录验证尚未完成。提供的是源码/静态/隔离数据库证据，不保证任何现场配置无需调整。

现场必须完成：成本DWS升级验收、中台页面权限保存、六种身份（含专员）的直接接口验权、预警闭环、型号比对、3D回退，以及中台已有模块的登录与访问回归。正式交付不等于生产验收完成。
